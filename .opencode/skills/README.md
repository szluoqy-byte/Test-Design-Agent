# OpenCode Skill Mirror

This directory is generated from the repository root `skills/` directory so OpenCode can discover the same skills that Claude Code loads from the plugin.

Do not edit files under `.opencode/skills/` directly. Edit `skills/*/SKILL.md`, then run:

```sh
python bin/sync-opencode-skills.py
```
